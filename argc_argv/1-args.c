#include "main.h"

/**
 * main - point entry
 * @argc: count arguments
 * @argv: argument values
 * Return: always 0 (Success)
 */

int main(int argc, char *argv[])
{
	if (argv[0])
	{
	}
	printf("%d\n", argc - 1);
	return (0);
}
