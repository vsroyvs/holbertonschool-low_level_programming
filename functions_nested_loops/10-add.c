#include "main.h"

/**
 * add - prints adds two numbers and returns the result
 *
 * @a: number will be added
 * @b: other number wil be added
 *
 * Return: Always a + b (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
