#include "main.h"

/**
 * _isdigit - if a character is a number
 *
 * @c: character will be examinated
 *
 * Return: returns 1 if is letter
 *      otherwise always 0 (Success)
 */

int _isdigit(int c)
{
	if (c > 47 && c < 58))
	{
		return (1);
	}
	else
	return (0);
}
