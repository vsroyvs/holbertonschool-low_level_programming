#include "main.h"

/**
 * mul - prints multiplies two numbers and returns the result
 *
 * @a: number will be multiplied
 * @b: other number wil be mulplied
 *
 * Return: Always a * b (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
